{
  "name": "dongsoop-frontend-deploy",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm --prefix frontend ci && npm --prefix frontend run build && node scripts/build-sites-dist.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
